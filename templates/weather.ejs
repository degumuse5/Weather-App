<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Weather Card</title>
  <style>
    body, html {
      height: 100%;
      margin: 0;
      font-family: Arial, Helvetica, sans-serif;
      background: linear-gradient(135deg, #e0eafc 0%, #cfdef3 100%);
    }
    .weather-container {
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100%;
    }
    .weather-card {
      border-radius: 20px;
      background: white;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      padding: 40px;
      max-width: 800px;
      width: 100%;
    }
    .weather-icon {
      width: 150px;
      margin-bottom: 20px;
    }
    .weather-details {
      color: #868b94;
      font-size: 1.2rem;
    }
    .location-time {
      color: #333;
      font-weight: bold;
    }
    .temperature {
      font-size: 4rem;
      font-weight: bold;
      color: #333;
    }
  </style>
</head>
<body>
  <div class="weather-container">
    <div class="weather-card text-center">
      <div class="location-time">
        <h2><%= location %></h2>
      </div>
      <div class="temperature">
        <%= temperature %>°F
      </div>
      <p class="weather-details mb-4"> Condition = <%= condition %></p>
      <div class="row">
        <div class="col-4">
          <i class="fas fa-wind weather-details"></i>
          <p class="weather-details"> WindSpeed = <%= windSpeed %> km/h</p>
        </div>
        <div class="col-4">
          <i class="fas fa-tint weather-details"></i>
          <p class="weather-details">Humidity = <%= humidity %>%</p>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
